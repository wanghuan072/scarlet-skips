import type { Metadata } from "next";
import EndingRoutePage from "@/page/run-lab/EndingRoutePage";
import { createMetadata } from "@/seo/metadata";
export const metadata: Metadata = createMetadata({title:"Scarlet Skips Ending Route Tracker",description:"Follow and locally save a spoiler-light Scarlet Skips ending checklist based on a documented community route.",path:"/run-lab/ending-route"});
export default EndingRoutePage;
