import type { Metadata } from "next";
import HomePage from "@/page/home/HomePage";
import { createMetadata } from "@/seo/metadata";

export const metadata: Metadata = createMetadata({title:"Scarlet Skips Guide – Upgrades, Ending & High Scores",description:"Learn Scarlet Skips controls, compare documented upgrades, plan goal-based builds and follow source-labelled ending and high-score strategies.",path:"/"});
export default HomePage;
