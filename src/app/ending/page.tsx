import type { Metadata } from "next";
import EndingPage from "@/page/ending/EndingPage";
import { createMetadata } from "@/seo/metadata";
export const metadata: Metadata = createMetadata({title:"Scarlet Skips Ending Guide – Spoiler-Light Route",description:"Reach the Scarlet Skips ending with a spoiler-light, versioned community route and an optional final trigger reveal.",path:"/ending",image:"/images/editorial/ending-hero.webp"});
export default EndingPage;
